/**
 * Library package preset — base config tuned for `packages/*`. Forbids
 * importing from `apps/*` (libraries must not depend on applications).
 */
import base from "./base.mjs";

export default base;
